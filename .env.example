# Example environment variables for Vercel KV configuration
# Copy this file to .env.local for local development

# Vercel KV (Redis) Configuration 
# Replace with your actual values from Vercel Dashboard
KV_URL=redis://default:your-password@your-host:your-port
KV_REST_API_URL=https://your-region.kv.vercel-storage.com
KV_REST_API_TOKEN=your-api-token
KV_REST_API_READ_ONLY_TOKEN=your-read-only-token

# IMAP Sync Configuration
# Default maximum message size to process (in bytes, default 26214400 = 25MB)
MAX_MESSAGE_SIZE=26214400

# Default maximum mailboxes to sync (set to 0 for unlimited)
MAX_MAILBOXES=0
